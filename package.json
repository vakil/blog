{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A lightweight website built with HTML, CSS, and vanilla JavaScript, featuring a blog system with Markdown support.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "dev": "nodemon --watch src --watch server.js --watch build.js --ext js,md,css --exec \"npm run build && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "front-matter": "^4.0.2",
    "fs-extra": "^11.3.0",
    "marked": "^15.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
